backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

# Uncomment below to enable drafts
# publish_mode: editorial_workflow

media_folder: "static/*" # Media files will be stored in the repo under images/uploads

collections:
  # Our blog posts
  # - name: "blog" # Used in routes, e.g., /admin/collections/blog
  #   label: "Post" # Used in the UI
  #   folder: "posts" # The path to the folder where the documents are stored
  #   create: true # Allow users to create new documents in this collection
  #   slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  #   fields: # The fields for each document, usually in front matter
  #     - { label: "Title", name: "title", widget: "string" }
  #     - { label: "Publish Date", name: "date", widget: "datetime" }
  #     - { label: "Summary", name: "summary", widget: "text" }
  #     - { label: "Tags", name: "tags", widget: "list", default: ["post"] }
  #     - { label: "Body", name: "body", widget: "markdown" }
  # Our pages e.g. About
  - name: "pages"
    label: "Page"
    folder: "pages"
    create: false # Change to true to allow editors to create new pages
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Permalink", name: "permalink", widget: "string" }
      - { label: "Navigation Title", name: "navtitle", widget: "string" }
      - { label: "Tags", name: "tags", widget: "hidden", default: "nav" }
      - { label: "Body", name: "body", widget: "markdown" }
  - label: "Globals"
    name: "globals"
    files:
      - label: "Recordings"
        name: "recordings"
        delete: false
        file: "_data/recordings.json"
        fields:
          - {label: "Titel", name: "title", widget: "string"}
          - {label: "Komponist", name: "composer", widget: "string"}
          - {label: "Sänger/In", name: "singer", widget: "string"}
          - {label: "mp3", name: "mp3", widget: "string"}
          - {label: "ogg", name: "ogg", widget: "string"}
      # - name: "songs"
      #   label: "Song"
      #   folder: "music"
      #   create: true # Change to true to allow editors to create new pages
      #   slug: "{{slug}}"
      #   fields:
      #     - { label: "Title", name: "title", widget: "string" }
      #     - { label: "SingerIn", name: "singer", widget: "string" }
      #     - { label: "Composer", name: "composer", widget: "string" }
      #     - { label: "File", name: "mp3", widget: "string" }
  # - name: "events"
  #   label: "Programme Entries"
  #   folder: "pages/sections/programme"
  #   create: true # Change to true to allow editors to create new pages
  #   slug: "{{slug}}"
  #   fields:
  #     - { label: "Title", name: "title", widget: "string" }
  #     - { label: "Publish Date", name: "date", widget: "datetime" }
  #     - { label: "Permalink", name: "permalink", widget: "string" }
  #     - { label: "Navigation Title", name: "navtitle", widget: "string" }
  #     - { label: "Tags", name: "tags", widget: "hidden", default: "nav" }
  #     - { label: "Body", name: "body", widget: "markdown" }
