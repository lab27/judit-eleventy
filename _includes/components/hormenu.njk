<nav class="horizontal-menu">
  <ul>
  {%- for nav in collections.nav | reverse -%}
    <li>
      <a href="{{ nav.url | url }}" data-navtitle="{{nav.data.section}}">{{ nav.data.navtitle }}</a>
      {% if loop.last %}

      {% else %}
      <span class="divider">|</span>
      {% endif %}
    </li>
  {%- endfor -%}
  </ul>
</nav>
