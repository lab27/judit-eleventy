<section class="quotes">

<article>
    <h3 class="u-centered">Lob</h3>
     {% for quote in metadata.quotes %}
        <div class="quote">
        <blockquote>
        <span class="big-quote">"</span>
        {{quote.text}}
        <span class="big-quote">"</span>
        </blockquote>
        <div class="quote-author">
        {% if quote.link%}<a href="{{quote.link}}" target="_blank">{%endif%}
        <p>- {{quote.author}}</p>
        {% if quote.link%}</a>{%endif%}
        </div>
        </div>
    
        {% endfor %}
    
        
</article>
</section>